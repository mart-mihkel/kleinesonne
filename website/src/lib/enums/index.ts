export enum Gender {
    MALE = "Male",
    FEMALE = "Female",
}

export enum Breed {
    AUSTRALIAN = "Australian",
    BERNESE = "Bernese",
    ENTLEBUCH = "Entlebuch",
}

export enum Availability {
    AVAILABLE = "Available",
    UNAVAILABLE = "Unavailable",
    CO_OWNERSHIP = "Co",
}
